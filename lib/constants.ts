// Default password assigned to every new user account created through the
// platform (super admin, schools, signup approvals, admin/director user
// creation flows, etc.). Users can change it from their profile page.
export const DEFAULT_USER_PASSWORD = "Password@2026!";
